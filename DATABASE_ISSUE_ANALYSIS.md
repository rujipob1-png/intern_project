# ปัญหา: ข้อมูลในระบบไม่ตรงกับ Database

## 🔍 ปัญหาที่พบ

ข้อมูลที่แสดงในระบบยังคงใช้ข้อมูลเก่าตั้งแต่เริ่มแรก ไม่ตรงกับโครงสร้าง database ที่อัพเกรดแล้ว

### สาเหตุ:
ในระบบมี **2 ฉบับ sample data ที่ต่างกัน**:

1. **`sample_data.sql`** (เก่า) - ใช้โครงสร้า schema เก่า
   - `department` เป็นคอลัมน์ TEXT ปกติ (ไม่ใช่ FK)
   - มี 4 roles เท่านั้น
   - Roles: user, director, central_office, admin
   - ข้อมูลตัวอย่าง:
     - EMP001: นายสมชาย ใจดี (ตำแหน่ง: เจ้าพนักงานธุรการ, กองบริหารทั่วไป)
     - อื่น ๆ

2. **`sample_data_4_levels.sql`** (ใหม่) - ใช้โครงสร้า schema ที่อัพเกรดแล้ว
   - `department_id` เป็น FK ไปยัง `departments` table
   - มี 5 roles
   - Roles: user, director, central_office_staff, central_office_head, admin
   - ข้อมูลตัวอย่าง:
     - EMP001: นายสมชาย ใจดี (ตำแหน่ง: นักวิเคราะห์ระบบ, กอง IT)
     - EMP003: นางจินตนา ดูแลดี (ตำแหน่ง: เจ้าหน้าที่ทรัพยากรบุคคล, กอง HR)

### ผลกระทบ:
- Dashboard แสดงข้อมูลจาก `sample_data.sql` เก่า
- Leave balance, department info ไม่ตรงกับ schema ใหม่
- Role levels อาจไม่ถูกต้องสำหรับระบบ 4 ระดับการอนุมัติ

---

## 🔧 วิธีแก้ไข

### ตัวเลือก 1: ใช้ Sample Data ใหม่ (แนะนำ)

ลบข้อมูลเก่าทั้งหมดและสร้างใหม่จาก `sample_data_4_levels.sql`:

```bash
# 1. ไปยัง directory database
cd backend

# 2. Reset database และสร้างใหม่
npm run db:setup:4-levels
# หรือ
npm run migrate

# 3. หรือสร้างจาก SQL files โดยตรง
# - รัน schema.sql ก่อน
# - แล้ว sample_data_4_levels.sql
```

### ตัวเลือก 2: อัพเดท Backend Controller

หากต้องการใช้ข้อมูลเก่าต่อไป ให้ตรวจสอบและอัพเดท:
- [backend/src/controllers/auth.controller.js](backend/src/controllers/auth.controller.js) - ตรวจสอบ join กับ departments
- [backend/src/controllers/leave.controller.js](backend/src/controllers/leave.controller.js) - ตรวจสอบ leave types

---

## ✅ Checklist หลังแก้ไข

- [ ] ลบข้อมูลเก่าทั้งหมด
- [ ] รัน schema.sql เพื่อสร้าง tables ใหม่
- [ ] รัน sample_data_4_levels.sql เพื่อสร้างข้อมูลตัวอย่าง
- [ ] Login ด้วย EMP001 / 123456
- [ ] ตรวจสอบ leave balance แสดง: ลาป่วย 30/30, ลาพักผ่อน 10/10, ลากิจ 3/3
- [ ] ตรวจสอบ department แสดง "กองเทคโนโลยีสารสนเทศ" (IT)
- [ ] ดูข้อมูลลาประวัติที่ถูกต้อง

---

## 📌 Script ใหม่ที่ควรเพิ่ม (ถ้าไม่มี package.json script)

```json
{
  "scripts": {
    "db:setup": "node setup-database.js",
    "db:setup:4-levels": "node migrate.js",
    "db:reset": "npx supabase db push --local",
    "db:seed": "node -e 'import(\".src/scripts/seed.js\")'"
  }
}
```

---

## 🚀 แนะนำการดำเนินการในขั้นตอน

1. **ทันทีที่นี้**: ลบข้อมูลเก่า
   ```bash
   npm run db:setup:4-levels
   ```

2. **ทดสอบ**: Login และตรวจสอบข้อมูลทั้งหมด

3. **อัพเดท**: Frontend ถ้ามีส่วนที่ reference ข้อมูลเก่า

