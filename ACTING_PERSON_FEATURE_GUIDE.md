# คู่มือการใช้งานฟีเจอร์ผู้ปฏิบัติหน้าที่แทน

## สรุปฟีเจอร์ที่เพิ่มเข้ามา

### 1. ฐานข้อมูล (Database)
ไฟล์: `database/add_acting_person_feature.sql`

**ความสามารถ:**
- เพิ่มคอลัมน์ `acting_person_id`, `acting_status`, `acting_approved_at`, `acting_comment` ในตาราง `leaves`
- สร้างตาราง `notifications` สำหรับจัดเก็บการแจ้งเตือน
- สร้าง Function `create_acting_notification()` ที่ทำงานอัตโนมัติเมื่อมีการเลือกผู้แทน
- สร้าง Function `approve_acting_person()` สำหรับอนุมัติการเป็นผู้แทน
- สร้าง View `notifications_detail` สำหรับดูการแจ้งเตือนพร้อมรายละเอียด

**วิธีติดตั้ง:**
```bash
# เข้าไปที่โฟลเดอร์ database
cd database

# รัน SQL script (ใช้ Supabase SQL Editor หรือ psql)
psql -h [your-host] -U [your-user] -d [your-database] -f add_acting_person_feature.sql
```

หรือใช้ Supabase Dashboard:
1. เปิด Supabase Dashboard
2. ไปที่ SQL Editor
3. Copy โค้ดจาก `add_acting_person_feature.sql` แล้ว Run

---

### 2. Backend API
ไฟล์ที่สร้างใหม่:
- `backend/src/controllers/acting.controller.js` - Controller สำหรับจัดการ API
- `backend/src/routes/acting.routes.js` - Routes สำหรับ API endpoints

**API Endpoints:**
- `GET /api/acting/same-level-employees` - ดึงรายชื่อพนักงานในชั้นเดียวกัน
- `GET /api/acting/requests` - ดึงคำขอที่ต้องอนุมัติ
- `POST /api/acting/requests/:leaveId/approve` - อนุมัติการเป็นผู้แทน
- `GET /api/acting/notifications` - ดึงการแจ้งเตือน
- `PUT /api/acting/notifications/:notificationId/read` - ทำเครื่องหมายว่าอ่านแล้ว
- `PUT /api/acting/notifications/read-all` - ทำเครื่องหมายว่าอ่านทั้งหมด

**ไฟล์ที่แก้ไข:**
- `backend/src/app.js` - เพิ่ม import และ route สำหรับ acting API

---

### 3. Frontend

#### Components ใหม่:
1. **ActingPersonSelect** (`frontend/src/components/leave/ActingPersonSelect.jsx`)
   - Component สำหรับเลือกผู้ปฏิบัติหน้าที่แทน
   - มีฟีเจอร์ search และ autocomplete
   - แสดงเฉพาะพนักงานในชั้นเดียวกัน

2. **NotificationBell** (`frontend/src/components/common/NotificationBell.jsx`)
   - กระดิ่งแจ้งเตือนที่ Header
   - แสดงจำนวนการแจ้งเตือนที่ยังไม่อ่าน
   - Dropdown แสดงการแจ้งเตือนล่าสุด
   - Auto-refresh ทุก 30 วินาที

#### Pages ใหม่:
1. **ActingRequestsPage** (`frontend/src/pages/user/ActingRequestsPage.jsx`)
   - หน้าสำหรับดูและอนุมัติคำขอให้ปฏิบัติหน้าที่แทน
   - แสดงรายละเอียดการลา
   - ปุ่มยอมรับการปฏิบัติหน้าที่แทน

#### API Client:
- `frontend/src/api/acting.api.js` - Functions สำหรับเรียก API

#### ไฟล์ที่แก้ไข:
- `frontend/src/components/layout/Header.jsx` - เพิ่ม NotificationBell

---

## วิธีใช้งาน

### สำหรับพนักงานที่ขอลา:
1. เมื่อกรอกฟอร์มขอลา จะเห็นฟิลด์ "ผู้ปฏิบัติหน้าที่แทน"
2. คลิกที่ฟิลด์แล้วพิมพ์ชื่อหรือรหัสพนักงาน
3. เลือกพนักงานที่ต้องการ (แสดงเฉพาะคนในกอง/ชั้นเดียวกัน)
4. ส่งคำขอลาตามปกติ

### สำหรับผู้ที่ถูกเลือกให้เป็นผู้แทน:
1. จะได้รับการแจ้งเตือนที่กระดิ่ง (มีจุดแดงและตัวเลข)
2. คลิกที่กระดิ่งเพื่อดูการแจ้งเตือน
3. คลิกที่การแจ้งเตือนเพื่อไปหน้ารายละเอียด
4. กดปุ่ม "ยอมรับการปฏิบัติหน้าที่แทน"
5. ระบบจะแจ้งกลับไปหาผู้ขอลาว่าได้รับการยอมรับแล้ว

---

## การเพิ่มฟิลด์ในฟอร์มการลา

ต้องแก้ไขไฟล์ที่มีฟอร์มการลา (เช่น `LeaveRequestForm.jsx` หรือหน้าที่ใช้ยื่นคำขอลา):

```jsx
import { ActingPersonSelect } from '../../components/leave/ActingPersonSelect';

// ใน component
const [formData, setFormData] = useState({
  // ... ฟิลด์อื่นๆ
  actingPersonId: null,
});

// ใน JSX
<ActingPersonSelect
  value={formData.actingPersonId}
  onChange={(value) => setFormData(prev => ({ ...prev, actingPersonId: value }))}
  required={true} // หรือ false ตามต้องการ
/>

// เมื่อ submit
const handleSubmit = async () => {
  const leaveData = {
    // ... ข้อมูลอื่นๆ
    acting_person_id: formData.actingPersonId,
  };
  // ส่งไปที่ API
};
```

---

## การเพิ่ม Route ในระบบ

เพิ่มใน routing config:

```jsx
import ActingRequestsPage from './pages/user/ActingRequestsPage';

// ใน Routes
<Route path="/acting-requests" element={<ActingRequestsPage />} />
```

---

## ข้อควรทราบ

1. **Database Migration**: ต้องรัน SQL script ก่อนใช้งาน
2. **Backend**: API ใช้ ES modules (import/export) ไม่ใช่ CommonJS (require)
3. **Frontend**: Components พร้อมใช้งาน แต่ต้องนำไปใส่ในฟอร์มการลา
4. **Notification**: Auto-refresh ทุก 30 วินาที ไม่ต้องทำอะไร
5. **Permission**: ทุกคนสามารถดูการแจ้งเตือนของตัวเองได้ ไม่มีการจำกัด role

---

## Troubleshooting

### ถ้า API ไม่ทำงาน:
1. ตรวจสอบว่ารัน migration SQL แล้ว
2. ตรวจสอบ Backend console มี error ไหม
3. ตรวจสอบว่า Supabase connection ใช้งานได้

### ถ้าไม่แสดงพนักงาน:
1. ตรวจสอบว่า user มี `department_id` และ `role_id` หรือไม่
2. ตรวจสอบว่ามีพนักงานคนอื่นในกอง/ชั้นเดียวกันหรือไม่
3. ดู Network tab ใน browser DevTools

### ถ้าไม่มีการแจ้งเตือน:
1. ตรวจสอบว่า Trigger ใน database ทำงานหรือไม่
2. ตรวจสอบตาราง `notifications` ว่ามีข้อมูลหรือไม่
3. Refresh หน้าเว็บใหม่

---

## สรุปไฟล์ทั้งหมด

### Database:
- `database/add_acting_person_feature.sql` ⭐ (รันก่อนใช้งาน)

### Backend:
- `backend/src/controllers/acting.controller.js` ⭐ (สร้างใหม่)
- `backend/src/routes/acting.routes.js` ⭐ (สร้างใหม่)
- `backend/src/app.js` (แก้ไข - เพิ่ม import และ route)

### Frontend:
- `frontend/src/api/acting.api.js` ⭐ (สร้างใหม่)
- `frontend/src/components/leave/ActingPersonSelect.jsx` ⭐ (สร้างใหม่)
- `frontend/src/components/common/NotificationBell.jsx` ⭐ (สร้างใหม่)
- `frontend/src/pages/user/ActingRequestsPage.jsx` ⭐ (สร้างใหม่)
- `frontend/src/components/layout/Header.jsx` (แก้ไข - เพิ่ม NotificationBell)

---

## ขั้นตอนการติดตั้งทั้งหมด

1. รัน SQL migration: `add_acting_person_feature.sql`
2. Restart Backend server
3. ใช้งาน Frontend (components พร้อมใช้แล้ว)
4. เพิ่ม `ActingPersonSelect` ในฟอร์มการลา
5. เพิ่ม route `/acting-requests` ในระบบ
6. ทดสอบการใช้งาน

เสร็จสิ้น! 🎉
